using System;
using Teigha.Runtime;

namespace MountingPlatePlugin.Builder
{
    class Program
    {
        [STAThread]
        static void Main(string[] args)
        {
            // Точка входа для отладки как отдельного приложения
            Console.WriteLine("Это библиотека для nanoCAD, запускайте через NETLOAD");
        }
    }
}